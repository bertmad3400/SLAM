Install dialog and arch keyrings
Configure system (keyboard and time)
UEFI vs BIOS
Partitioning (root and boot)
Filesystems
Swapfile
Configure mirrorlist with reflector

pacstrap /mnt base linux linux-firmware
genfstab -U /mnt >> /mnt/etc/fstab

Chroot
Install dialog and arch keyrings
Select timezone
Set hostname
Enable dhcpcd
Root account password
Setup normal user
Install and setup doas
Install git
Install YAY helper

