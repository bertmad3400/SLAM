Fix swap file creation
Give language options for keyboard during setup
Remember to check if drives are mounted
Change how bootloader is configured (use kernel as bootloader for uefi)
Add option for full disk encryption
